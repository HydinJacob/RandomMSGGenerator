Random Message Generator

Generate a random mesage when a user click on the button provided.
The message should contain a greeting, inspiration thought and thank you!


Technologies were used Javascript, HTML, and CSS.

The project can be run from a browser using the index.html file or can be run on the Node.js command prompt (use the javascript file).
